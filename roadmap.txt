(/) Rename const_array to array_view
( ) Get std::complex compiling
( ) Add code coverage
( ) Verify I implemented move semantics correctly
( ) Replace local macros with lambdas
( ) Add bazel build system, pull in libfmt and catch
( ) Don't use std::vector<R> as the underlying storage type
( ) Make array_view it's own class, array derives from it
